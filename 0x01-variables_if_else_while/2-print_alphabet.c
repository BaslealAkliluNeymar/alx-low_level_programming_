#include <stdio.h>

/**
* main - Entry point
*
* Return: Always 0 (Success/correct)
*/

int main(void)
{

char alphabet;

for(alphabet = Ã'a' ; alphabet <= 'z' ; alphabet++)
{
putchar(alphabet);
}

putchar('\n');

return (0);

}

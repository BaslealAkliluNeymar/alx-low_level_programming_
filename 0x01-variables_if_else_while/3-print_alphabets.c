#include <stdio.h>

/**
* main - Entry point
*
* Return: Always 0 (Success/correct)
*/

int main(void)
{

char lower;
char upper;

for (lower = Ã'a'; lower <= 'z'; lower++)
{
putchar(lower);
}

for (upper =  'A'; upper <= 'A'; upper++)
{
putchar(upper);
}

putchar('\n');

return (0);

}

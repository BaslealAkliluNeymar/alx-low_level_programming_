#include "main.h"
/**
* islower - picks the lower and sends


* Description - finds the lowest and returns 0 or 1
*/
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
{
return (0);
}
else
{
return (1);
}
D